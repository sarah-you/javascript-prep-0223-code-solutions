/* exported isVowel */
function isVowel(char) {
  if (char === 'a' || char === 'A' || char === 'E' || char === 'e' || char === 'O' || char === 'o' || char === 'U' || char === 'u' || char === 'I' || char === 'i') {
    return true;
  } else {
    return false;
  }

}
isVowel();
